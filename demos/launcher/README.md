# CODA Demo Launcher

这是本阶段的统一视觉入口，使用浏览器打开 `index.html` 即可查看六个 Demo 的能力卡、四区证据布局、D3 真实 GDBot 截图和当前未完成项。

它不替代各 Demo 的运行验证，也不把离线 fixture 当作真实物理或硬件 benchmark。离线报告使用：

```sh
npm run demo:smoke
```

完整离线入口（含 D3 Godot headless smoke）：

```sh
npm run demo:acceptance
```
